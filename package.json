{
  "name": "wonderreel",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,web -c blue,green \"npm run start --prefix backend\" \"npm run dev --prefix web -- -p 3005\"",
    "dev:backend": "npm run start --prefix backend",
    "dev:web": "npm run dev --prefix web -- -p 3005"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
